{% extends 'base.html.twig' %}



{% block title %}SNN Networks || Single Post {% endblock %}


{% block body %}

<body>

<section id="main">
    <div class="container">
        <article id="main-col">

            <br/>
            <br/>
            <h1 class="page-title" style="font-family:Arial;color: #222222" ><b>{{ post.title }}</b></h1>
            <h4 class="authors" style="color:grey">Author:::{{ auth_name }}</h4> &nbsp;
            {% if post.image==null %}
                <p >#####image not found for this post#####</p>
            {% else %}
                <li><img src="{{ vich_uploader_asset(post, 'imageFile')}}" alt="new" style="width:400px;height:400px;"/></li>
            {% endif %}
            &nbsp;&nbsp;
            <p>   {{ post.article }}    </p>

            <br/>
            <br/>
            <br/>
            <br/>
            <br/>

            <section id="post_box">
                <div class="container">

                    {% if is_granted('ROLE_Author') %}
                    <form class="ui form" action="/blog/delete/{{ post.id }}" method="get" enctype="multipart/form-data">
                        <button class="ui button" type="submit">Delete Article</button>

                    {% endif %}





                    </form>
                </div>

            </section>

{% endblock %}












{#
{% extends 'base.html.twig' %}

{% block title %}Post By Id!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello ! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>id is -->{{ post.id }}</li>
            <li>title is -->{{ post.title }} </li>
            <li>Content is -->{{ post.article }}</li>
            <li>Author name is -->{{ auth_name }}</li>

            {% if post.image==null %}
            <li>image not found for this post</li>
            {% else %}
                <li><img src="{{ vich_uploader_asset(post, 'imageFile')}}" alt="new" style="width:400px;height:400px;"/></li>
            {% endif %}
        </ul>
    </div>
{% endblock %}

#}